name: posture_detection_and_correction
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  clipboard: ^0.1.3
  flutter:
    sdk: flutter
  light: ^3.0.1
  sensors_plus: ^5.0.1
  # flutter_use_sensors: ^0.0.4
  # flutter_use: ^0.0.4
  # flutter_hooks: ^0.20.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
